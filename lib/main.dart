// Import helper lib from flutter to get content on the screen
import 'package:flutter/material.dart';
import 'src/app.dart';

// Define 'main' func to run when our app starts
void main() {
  // Create a new text widget to show some text on the screen

  // Take that widget and get it on the screen
  runApp(App());
}
